# Dependencies
node_modules/
bun.lockb

# Build output
server/dist/
sourcemap.json

# Keep the built plugin tracked so the installer can fetch it
!TufanBloxBridge.rbxm

# Plugin builder binary (argon/rojo) — fetched locally, not committed (~10 MB).
# Root-anchored (leading /) so it does NOT also match server/src/mcp/tools/.
/tools/


# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store
*.log

# internal research — never publish
docs/responsive-ui-research.md
*-research.md
_private/
